// Define a variable named roundUp that references the float 1.5.
var roundUp = 1.5;

// Use the Math.round() method to round the number up.
var rounded = Math.round(roundUp);

// Print the rounded number to the terminal.
console.log(rounded);
